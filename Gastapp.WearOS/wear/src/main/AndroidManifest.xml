<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:name=".GastappApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">

        <uses-library
            android:name="com.google.android.wearable"
            android:required="true" />

        <!--
            El reloj funciona sin la app del telefono una vez vinculado: habla
            directo con la API por HTTPS.
        -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity
            android:name=".presentation.MainActivity"
            android:exported="true"
            android:taskAffinity=""
            android:theme="@style/MainActivityTheme.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Captura por voz. La lanza el tile y tambien la pantalla principal. -->
        <activity
            android:name=".ui.quickadd.QuickAddActivity"
            android:exported="true"
            android:taskAffinity=""
            android:label="@string/quick_add_label"
            android:theme="@android:style/Theme.DeviceDefault" />

        <!--
            Canal con la app del telefono. Play Services lo arranca al llegar un
            mensaje, incluso con la app cerrada: por eso la revocacion desde el
            telefono se nota al instante y no cuando rebota un 401.
        -->
        <service
            android:name=".data.wear.GastappWearListenerService"
            android:exported="true">
            <intent-filter>
                <!--
                    Hacen falta las dos acciones. Solo con MESSAGE_RECEIVED, el
                    onDataChanged del servicio no se invoca nunca y los datos que
                    empuja el telefono se pierden sin ningun error.
                -->
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data android:scheme="wear" android:host="*" android:pathPrefix="/gastapp" />
            </intent-filter>
        </service>

        <!--
            Sin android:permission el sistema ignora el service en silencio y el tile
            nunca aparece en la lista.
        -->
        <service
            android:name=".tile.ExpenseTileService"
            android:label="@string/tile_label"
            android:description="@string/tile_description"
            android:exported="true"
            android:permission="com.google.android.wearable.permission.BIND_TILE_PROVIDER">
            <intent-filter>
                <action android:name="androidx.wear.tiles.action.BIND_TILE_PROVIDER" />
            </intent-filter>

            <meta-data
                android:name="androidx.wear.tiles.PREVIEW"
                android:resource="@drawable/tile_preview" />
        </service>

    </application>

</manifest>
